<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ejerciciosmas40">

    <application
        android:allowBackup="true"
        android:icon="@drawable/dino"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WelcomeActivity"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".UserPersonalDataActivity"
            android:parentActivityName=".WelcomeActivity"/>

        <activity android:name=".ExerciseFrequencyActivity"
            android:parentActivityName=".UserPersonalDataActivity"/>

        <activity android:name=".ExerciseTypeActivity"
            android:parentActivityName=".ExerciseFrequencyActivity"/>

    </application>

</manifest>